import * as THREE from 'three';



export function IaGangster(gangster, camera, setPlayerHealth, deltaTime) {
    if (!gangster) return;

    const distanceToplayerAndGangster = gangster.position.distanceTo(camera.position);



}